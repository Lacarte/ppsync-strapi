{
  "kind": "collectionType",
  "collectionName": "doctypes",
  "info": {
    "name": "doctype"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "description": {
      "type": "string",
      "required": true,
      "unique": true
    },
    "state": {
      "type": "enumeration",
      "enum": [
        "actif",
        "inactif",
        "eliminer"
      ]
    }
  }
}
