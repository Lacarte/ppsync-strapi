{
  "kind": "collectionType",
  "collectionName": "user_profiles",
  "info": {
    "name": "user-profile"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "description": {
      "type": "string",
      "required": true,
      "unique": true
    },
    "state": {
      "type": "enumeration",
      "enum": [
        "actif",
        "inactif",
        "eliminer"
      ],
      "required": true,
      "default": "actif"
    }
  }
}
